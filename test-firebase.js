import admin from 'firebase-admin'; import { readFileSync } from 'fs'; try { const serviceAccount = JSON.parse(readFileSync('serviceAccountKey.json', 'utf8')); console.log('? Service account loaded for:', serviceAccount.project_id); admin.initializeApp({ credential: admin.credential.cert(serviceAccount) }); console.log('? Firebase initialized successfully'); } catch (error) { console.error('? Error:', error.message); } 
